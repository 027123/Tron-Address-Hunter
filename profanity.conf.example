# Tron Address Hunter configuration file
# Copy this file to profanity.conf and modify as needed.
# Command line arguments override config file values.

# Matching input: file path or single address
matching=profanity.txt

# Minimum number of prefix matches (0-10, default 0)
# prefix-count=0

# Minimum number of suffix matches (0-10, default 6)
suffix-count=6

# Exit after finding this many results (0 = unlimited)
quit-count=10

# Output results to file (supports {date} placeholder, expands to YYYYMMDD_HHMM)
output=result_{date}.txt

# Local work size (default 128)
# work=128

# Skip GPU device by index (e.g. skip integrated GPU)
# No longer needed by default - the program auto-detects and skips
# incompatible GPUs from different OpenCL platforms.
# skip=1
